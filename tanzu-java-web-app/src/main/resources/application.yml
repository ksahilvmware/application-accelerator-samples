spring:
  application:
    name: tanzu-java-web-app
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST}/postgres
    username: masteruser
    password: ${POSTGRES_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
management:
  endpoints:
    web:
      exposure:
        include: "*"  
  endpoint:
    health:
      show-details: always